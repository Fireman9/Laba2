#include<iostream>
#include<vector>
#include<string>

using namespace std;

void ssort(vector<string> team, vector<int> points) {
	int a = points.size();
	int b, c;
	for (int i = 0;  i < a;  i++)
	{
	}
}

int maxEl(vector<int> points) 